<?jelly escape-by-default='true'?>
<j:jelly xmlns:j="jelly:core" xmlns:f="/lib/form">

  <f:entry title="Secret key name" field="infisicalKey">
    <f:textbox placeholder="SECRET_KEY"/>
  </f:entry>

  <f:entry field="isRequired">
    <f:checkbox title="Fail if secret is not found?" default="${descriptor.DEFAULT_IS_REQUIRED}" />
  </f:entry>


  <f:entry title="">
    <div align="right">
      <f:repeatableDeleteButton/>
    </div>
  </f:entry>

</j:jelly>
